name: Iac Pipeline - Deploy with Terragrunt on GCP

on:
  push:
    branches:
    - develop
    - staging
    - main
  pull_request:
    branches:
    - develop
    - staging
    - main

env:
  dev_path: "environment/01-dev/"

jobs:
  develop:
    if: github.ref == 'refs/heads/develop'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v2
        with:
          terraform_version: 1.5.0
          terraform_wrapper: false
          
      - name: Install Terragrunt
        run: |
          curl -sSL https://github.com/gruntwork-io/terragrunt/releases/download/v0.38.1/terragrunt_linux_amd64 -o terragrunt
          chmod +x terragrunt
          sudo mv terragrunt /usr/local/bin/

      - name: Install Google Cloud SDK (gcloud)
        run: |
          echo "Installing Google Cloud SDK"
          curl https://sdk.cloud.google.com | bash
          source $HOME/google-cloud-sdk/path.bash.inc

      - name: Check terragrunt and terraform installation
        run: |
          which terragrunt
          terragrunt --version
          which terraform
          terraform --version
          file /usr/local/bin/terragrunt

      - name: Set up GCP authen
        run: |
          echo "${{ secrets.GCP_SA_KEY }}" | base64 --decode > "${HOME}/gcp-key.json"
          ls -la ${HOME}
          gcloud auth activate-service-account --key-file="${HOME}/gcp-key.json"
          gcloud config set project ${{ secrets.GCP_PROJECT_ID }}
        env:
          GOOGLE_APPLICATION_CREDENTIALS: "${HOME}/gcp-key.json"
        
      - name: Apply GCS (Bootstrap)
        run: |
          export GOOGLE_APPLICATION_CREDENTIALS="${HOME}/gcp-key.json"
          cd ${{ env.dev_path }}
          terragrunt apply -auto-approve --terragrunt-working-dir=00-gcs << EOF
          y
          EOF
        env:
          GOOGLE_APPLICATION_CREDENTIALS: "${HOME}/gcp-key.json"

      - name: Apply VPC (Virtual Private Cloud)
        run: |
          export GOOGLE_APPLICATION_CREDENTIALS="${HOME}/gcp-key.json"
          cd ${{ env.dev_path }}
          terragrunt apply -auto-approve --terragrunt-working-dir=01-vpc
        env:
          GOOGLE_APPLICATION_CREDENTIALS: "${HOME}/gcp-key.json"

      - name: Apply RDS (Cloud SQL)
        run: |
          export GOOGLE_APPLICATION_CREDENTIALS="${HOME}/gcp-key.json"
          cd ${{ env.dev_path }}
          terragrunt apply -auto-approve --terragrunt-working-dir=02-rds
        env:
          GOOGLE_APPLICATION_CREDENTIALS: "${HOME}/gcp-key.json"

      - name: Apply GKE (Google Kubernetes Engine)
        run: |
          export GOOGLE_APPLICATION_CREDENTIALS="${HOME}/gcp-key.json"
          cd ${{ env.dev_path }}
          terragrunt apply -auto-approve --terragrunt-working-dir=03-gke
        env:
          GOOGLE_APPLICATION_CREDENTIALS: "${HOME}/gcp-key.json"

      - name: Apply Helm
        run: |
          export GOOGLE_APPLICATION_CREDENTIALS="${HOME}/gcp-key.json"
          cd ${{ env.dev_path }}
          terragrunt apply -auto-approve --terragrunt-working-dir=04-helm
        env:
          GOOGLE_APPLICATION_CREDENTIALS: "${HOME}/gcp-key.json"

      - name: Apply Domain
        run: |
          export GOOGLE_APPLICATION_CREDENTIALS="${HOME}/gcp-key.json"
          cd ${{ env.dev_path }}
          terragrunt apply -auto-approve --terragrunt-working-dir=05-domain
        env:
          GOOGLE_APPLICATION_CREDENTIALS: "${HOME}/gcp-key.json"
